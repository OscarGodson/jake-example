var fs = require('fs');

/**
 * SETS UP PROJECT BY INSTALLING ALL DEPENDENCIES
 */
desc('Setups up project');
task('setup', function () {
  jake.exec('npm install', {printStdout: true, printStderr: true});
});
